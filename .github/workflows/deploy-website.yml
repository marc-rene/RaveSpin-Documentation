# Build the RaveSpin website (Documentation/Website) and deploy to GitHub Pages.
# After the first run: Repo → Settings → Pages → Source: Deploy from branch → Branch: gh-pages, / (root).
# Site will be at: https://<owner>.github.io/RaveSpin/

name: Deploy website

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install
        working-directory: Documentation/Website

      # Project page base path (e.g. username.github.io/RaveSpin). Omit for user/org site.
      - name: Build
        run: npm run build
        working-directory: Documentation/Website
        env:
          VITE_BASE_PATH: '/RaveSpin/'

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: Documentation/Website/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
