# RaveSpin-Documentation

Static showcase site for [RaveSpin](https://github.com/marc-rene/RaveSpin). This repo is the website only — build with **Node** or **Bun**. Deploy via GitHub Actions to GitHub Pages.

**Live (after you enable Pages):** `https://<owner>.github.io/RaveSpin-Documentation/`

---

## Build (Node or Bun)

From the **repo root**:

```bash
# With Node (v18+)
npm install
npm run build

# With Bun
bun install
bun run build
```

Output is in **`dist/`**. The contents of `dist/` are what gets deployed.

## Local preview

```bash
npm run preview   # or: bun run preview
```

Serves the built site from `dist/` so you can check links and layout before deploying.

---

## Deploy with GitHub Actions (recommended)

This repo includes a workflow that builds and deploys the site on every push to `main`.

1. **Push to `main`** — The workflow runs automatically.
2. **Enable GitHub Pages** (one-time):
   - Repo → **Settings** → **Pages**
   - **Source:** Deploy from a branch
   - **Branch:** `gh-pages` (or **GitHub Actions** if your account uses the new source)
   - Save

3. **Site URL:** `https://<owner>.github.io/RaveSpin-Documentation/`

The workflow uses **`npx vite build`** so the build runs correctly in CI (avoids “vite: Permission denied” on Linux). Base path is set to `/RaveSpin-Documentation/` for project-page URLs.

### If you renamed the repo

Edit **`.github/workflows/deploy-website.yml`** and change:

```yaml
env:
  VITE_BASE_PATH: '/YourActualRepoName/'
```

and in **`vite.config.js`** you can leave `base` as-is (it reads `VITE_BASE_PATH`).

---

## Copy to a different GitHub Pages repo

If you want to publish from another repo (e.g. your personal site):

1. Build: `npm run build` or `bun run build`
2. Copy **everything inside** `dist/` to the root of the target repo.
3. If that site is served at a **subpath** (e.g. `username.github.io/MySite/`), set `VITE_BASE_PATH=/MySite/` before building, or in `vite.config.js`: `base: '/MySite/'`.

---

## Repo structure

| Path | Description |
|------|-------------|
| **`src/`** | Source: `index.html`, `tech.html`, `features.html`, `about.html`, `style.css`, `main.js` |
| **`dist/`** | Generated by `npm run build` — deployable static site (gitignored or deployed only via Actions) |
| **`.github/workflows/deploy-website.yml`** | Builds and deploys to GitHub Pages |
| **`package.json`** | Scripts and Vite dependency |

---

## Content overview

- **Home** — Hero, short feature list, demo link, links to Tech and About
- **Tech** — Godot 4.6, OpenXR, Meta Quest, audio buses, beat FX, key scripts
- **Features** — Full feature list: virtual controller, dual decks, EQ, beat FX, LibreBox, metadata, passthrough, multiplayer (design)
- **About** — Project context, author (César Hannin, TU Dublin), main repo, demo video

The site is written so both **general visitors** and **tech/XR/Godot/FOSS enthusiasts** get a quick summary, with deeper detail on the Tech and Features pages.
