{
  "name": "ravespin-website",
  "version": "1.0.0",
  "private": true,
  "description": "RaveSpin project showcase site — build for GitHub Pages",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npx vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
